# Sourced by bash when started in interactive login mode, i.e.you ssh into 
# system, or use console mode (ctrl+alt+f1..f6)

# Load .profile, containing login, non-bash related initializations.
source ~/.profile

# Load .bashrc, containing non-login related bash initializations.
# source ~/.bashrc
